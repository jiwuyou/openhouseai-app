<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity android:name=".app.activities.OpenHouseHomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="github-oauth-callback"
                    android:scheme="operit" />
            </intent-filter>
        </activity>

        <activity
            android:name=".app.activities.OperitAssistantActivity"
            android:exported="false"
            android:label="@string/operit_assistant_title"
            android:theme="@style/Theme.TermuxMaintenance.DayNight.NoActionBar" />
    </application>
</manifest>
