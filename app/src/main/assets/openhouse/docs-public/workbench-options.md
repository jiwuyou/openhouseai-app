# 工作台选择

这是稳定小写入口。完整说明见 `WORKBENCH_OPTIONS.md`。

pi-agent 是首次配置助手和文档索引员，不是唯一主工作台。OpenHouse 的主工作台由用户自己选择，可以是 Claude Code、Codex、Hermes Web，也可以是用户让 AI 搜索、安装和改造的其他开源项目。

选择工作台时，优先确认：是否能由 service-manager 管理、是否能注册到侧边栏、是否能读写用户项目、是否有清晰的模型配置和排障方式。
