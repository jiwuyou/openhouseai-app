# OpenHouseAI 文档

本目录只保留两类说明：

1. 运行环境说明：见 `ENVIRONMENT.md`。
2. Codex、Claude Code 和 Reasonix 的登录/API 配置：见 `MODEL_API_SETUP.md`。

如果 Agent 需要先了解当前手机环境，应先读 `ENVIRONMENT.md`。如果用户要配置大模型服务、登录账号或设置 API key，应读 `MODEL_API_SETUP.md`。
